const url = 'http://ec2-54-167-181-205.compute-1.amazonaws.com:8080/ticket-guru/v1/api';
const customerUrl = `${url}/customers`;
const venuesUrl = `${url}/venues`;

module.exports = {
  url,
  customerUrl,
  venuesUrl
}